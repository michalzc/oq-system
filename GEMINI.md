# Project
It's a FoundryVTT system for OpenQuest RPG.

# Tools
- gulp
- rollup
- yarn

Do not run npm, use yarn istead.

# Code style
Functional approach with heavy use of `lodash` libary. Avoid mutable state as far as possiblie in this coursed framework.
Use `const` for creating constant values, avoid using `let`.

